<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>
        <!-- Inherit Form View to Modify it -->
        <record id="view_account_invoice_inherit_form" model="ir.ui.view">
            <field name="name">view.account.invoice.xiao.form</field>
            <field name="model">account.invoice</field>
            <field name="inherit_id" ref="account.invoice_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@string='Other Info']" position="before">
                    <page string="实开发票信息">
                        <group>
                            <field name="invoice_company_name"/>
                        </group>
                        <field name="real_invoice_ids">
                            <tree editable="top">
                                <field name="real_invoice_date"/>
                                <field name="real_invoice_number"/>
                                <field name="real_invoice_amount"/>
                                <field name="comment"/>
                            </tree>
                        </field>
                    </page>
                </xpath>
            </field>
        </record>
    </data>
</openerp>